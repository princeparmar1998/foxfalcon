import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Create Account | Fox Falcon",
  description: "Register for a Fox Falcon account to unlock VIP benefits, start earning loyalty stamps, and save custom streetwear designs.",
};

export default function RegisterLayout({ children }: { children: React.ReactNode }) {
  return children;
}
