import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Streetwear Collections | Fox Falcon",
  description: "Explore curated Fox Falcon collections. Browse luxury heavyweight blanks, graphic drops, and seasonal streetwear releases.",
};

export default function CollectionsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
